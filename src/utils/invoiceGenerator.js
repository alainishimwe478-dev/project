// import jsPDF from "jspdf";

export function generateInvoice(user, amount, service, date) {
  // PDF generation disabled - jsPDF not installed
  console.log(`Invoice for ${user}: RWF ${amount} - ${service} on ${date}`);
  // To enable: npm install jspdf
}